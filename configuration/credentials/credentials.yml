github-packages:
  type: usernamePassword
  data:
    username: "{{ env_vars.GITHUB_USER_NAME }}"
    password: "{{ env_vars.GITHUB_TOKEN }}"
consul-token:
  type: secret
  data:
    secret: "test-secret-value"
dbaas:
  type: userpass
  data:
    username: "testuser"
    password: "testpass"
maas:
  type: usernamePassword
  data:
    username: "test-maas-user"
    password: "test-maas-pass"